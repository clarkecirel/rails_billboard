<%= form_with(model: billboard, local: true, class: 'ui form') do |f| %>
  
  <div class='field'>
  <% f.label :chart %>
  <%= f.text_field :chart, required: true, placeholder: "Chart" %>
  
  
  </div>
  <%= f.submit %>

  <% end %>